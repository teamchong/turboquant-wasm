diff --git a/cmake/external/onnx b/cmake/external/onnx
index be2b5fd..e75ceb7 160000
--- a/cmake/external/onnx
+++ b/cmake/external/onnx
@@ -1 +1 @@
-Subproject commit be2b5fde82d9c8874f3d19328bdfe3b6962dc67b
+Subproject commit e75ceb770f62bea5a4252b6b3e6afecf7c716cb8
diff --git a/include/onnxruntime/core/session/environment.h b/include/onnxruntime/core/session/environment.h
index 338dbf8..f21f122 100644
--- a/include/onnxruntime/core/session/environment.h
+++ b/include/onnxruntime/core/session/environment.h
@@ -8,7 +8,7 @@
 #include <memory>
 #include <optional>
 #include <vector>
-#include <shared_mutex>
+#include <mutex>
 #include <string>
 
 #include "core/common/common.h"
@@ -304,7 +304,7 @@ class Environment {
   // Application-specified environment configuration entries
   // The environment may add or remove an entry on EP library registration and unregistration, respectively.
   OrtKeyValuePairs config_entries_;
-  mutable std::shared_mutex config_entries_mutex_;  // Should be locked when accessing config_entries_
+  mutable std::mutex config_entries_mutex_;  // Should be locked when accessing config_entries_
 
   // Tracks the number of registered EP libraries that can create virtual devices.
   // It is incremented when an EP library is registered with a name that ends in ".virtual".
diff --git a/onnxruntime/core/framework/graph_partitioner.cc b/onnxruntime/core/framework/graph_partitioner.cc
index cc65142..b427d44 100644
--- a/onnxruntime/core/framework/graph_partitioner.cc
+++ b/onnxruntime/core/framework/graph_partitioner.cc
@@ -933,9 +933,15 @@ static Status InlineFunctionsAOTImpl(const ExecutionProviders& execution_provide
 
 // Validate the ep_context_path to make sure it is file path and check whether the file exist already
 // TODO: Move function to ep_context_utils.h/cc
+#ifndef __wasm__
 static Status GetValidatedEpContextPath(const std::filesystem::path& ep_context_path,
+#endif
+#ifndef __wasm__
                                         const std::filesystem::path& model_path,
+#endif
+#ifndef __wasm__
                                         std::filesystem::path& context_cache_path,
+#endif
                                         bool error_if_output_file_exists = true) {
   if (!ep_context_path.empty()) {
     context_cache_path = ep_context_path;
@@ -953,7 +959,9 @@ static Status GetValidatedEpContextPath(const std::filesystem::path& ep_context_
     return ORT_MAKE_STATUS(ONNXRUNTIME, INVALID_ARGUMENT, "Both ep_context_path and model_path are empty.");
   }
 
+#ifndef __wasm__
   if (std::filesystem::exists(context_cache_path) && error_if_output_file_exists) {
+#endif
     return ORT_MAKE_STATUS(ONNXRUNTIME, FAIL, "Failed to generate EP context model since the file '",
                            context_cache_path, "' exists already. Please remove the EP context model if you want to re-generate it.");
   }
@@ -1005,12 +1013,20 @@ static Status CreateEpContextModel(const ExecutionProviders& execution_providers
 
   const epctx::BufferHolder* output_buffer_holder = ep_context_gen_options.TryGetOutputModelBuffer();
   const epctx::BufferWriteFuncHolder* output_write_func_holder = ep_context_gen_options.TryGetOutputModelWriteFunc();
+#ifndef __wasm__
   const std::filesystem::path* output_model_path_ptr = ep_context_gen_options.TryGetOutputModelPath();
+#endif
 
+#ifndef __wasm__
   std::filesystem::path valid_output_model_path;
+#endif
   if (output_model_path_ptr != nullptr || !graph.ModelPath().empty()) {
+#ifndef __wasm__
     std::filesystem::path output_model_path = (output_model_path_ptr != nullptr) ? *output_model_path_ptr
+#endif
+#ifndef __wasm__
                                                                                  : std::filesystem::path("");
+#endif
     ORT_RETURN_IF_ERROR(GetValidatedEpContextPath(output_model_path,
                                                   graph.ModelPath(),
                                                   valid_output_model_path,
@@ -1467,10 +1483,14 @@ Status GraphPartitioner::Partition(Graph& graph, FuncManager& func_mgr,
   if (mode == Mode::kNormal || mode == Mode::kAssignOnly) {
 #if !defined(ORT_MINIMAL_BUILD)
     if (ep_context_gen_options.enable) {
+#ifndef __wasm__
       if (const std::filesystem::path* output_model_path_ptr = ep_context_gen_options.TryGetOutputModelPath();
+#endif
           output_model_path_ptr != nullptr) {
         // Check before EP compile graphs
+#ifndef __wasm__
         std::filesystem::path context_cache_path;
+#endif
         ORT_RETURN_IF_ERROR(GetValidatedEpContextPath(*output_model_path_ptr, graph.ModelPath(),
                                                       context_cache_path,
                                                       ep_context_gen_options.error_if_output_file_exists));
diff --git a/onnxruntime/core/framework/resource_accountant.cc b/onnxruntime/core/framework/resource_accountant.cc
index 68610eb..c7df41a 100644
--- a/onnxruntime/core/framework/resource_accountant.cc
+++ b/onnxruntime/core/framework/resource_accountant.cc
@@ -199,7 +199,9 @@ void NodeStatsRecorder::DumpStats(const std::filesystem::path& model_path) const
     node_stats_file = node_stats_file.parent_path();
   }
   node_stats_file /= GetNodeStatsFileName();
+#ifndef __wasm__
   std::ofstream ofs(node_stats_file, std::ofstream::out);
+#endif
   ORT_ENFORCE(ofs.is_open(), "Failed to open file: ", node_stats_file);
   DumpStats(ofs);
   ofs.close();
diff --git a/onnxruntime/core/framework/tensorprotoutils.cc b/onnxruntime/core/framework/tensorprotoutils.cc
index e36be3f..9e044dd 100644
--- a/onnxruntime/core/framework/tensorprotoutils.cc
+++ b/onnxruntime/core/framework/tensorprotoutils.cc
@@ -8,6 +8,9 @@
 #include <limits>
 #include <string>
 #include <filesystem>
+#ifdef __wasm__
+namespace std { namespace filesystem { static inline bool exists(const std::string&) { return false; } static inline uintmax_t file_size(const std::string&) { return 0; } } }
+#endif
 #if defined(__wasm__)
 #include <emscripten.h>
 #endif
@@ -198,7 +201,9 @@ DEFINE_4BIT_UNPACK_TENSOR_WITH_RAW_DATA_IMPL(Float4E2M1x2, CalcNumFloat4Pairs)
 // then uses the current directory instead.
 // This function does not unpack string_data of an initializer tensor
 Status ReadExternalDataForTensor(const ONNX_NAMESPACE::TensorProto& tensor_proto,
+#ifndef __wasm__
                                  const std::filesystem::path& tensor_proto_dir,
+#endif
                                  std::vector<uint8_t>& unpacked_tensor) {
   ORT_RETURN_IF(utils::HasString(tensor_proto), "This function does not support string data");
   PathString external_file_path;
@@ -229,9 +234,13 @@ Status ReadExternalDataForTensor(const ONNX_NAMESPACE::TensorProto& tensor_proto
   // Validate that the external file is large enough before allocating.
   // This protects against a model with a huge declared shape but a missing/short external file.
   std::error_code fs_error_code{};
+#ifndef __wasm__
   std::uintmax_t file_length = std::filesystem::file_size(external_file_path, fs_error_code);
+#endif
   ORT_RETURN_IF(fs_error_code, "Failed to get file size for external initializer ", tensor_proto.name(),
+#ifndef __wasm__
                 ". std::filesystem error: ", fs_error_code.message(), " (value: ", fs_error_code.value(), ")");
+#endif
   SafeInt<onnxruntime::FileOffsetType> end_of_read(file_offset);
   end_of_read += tensor_byte_size;
   ORT_RETURN_IF(file_offset < 0 || static_cast<std::uintmax_t>(end_of_read) > file_length,
@@ -260,7 +269,9 @@ Status ReadExternalDataForTensor(const ONNX_NAMESPACE::TensorProto& tensor_proto
   return Status::OK();
 }
 
+#ifndef __wasm__
 Status TensorProtoToOrtValueImpl(const Env& env, const std::filesystem::path& model_path,
+#endif
                                  const ONNX_NAMESPACE::TensorProto& tensor_proto, const MemBuffer* m,
                                  AllocatorPtr alloc, OrtValue& value) {
   if (m && m->GetBuffer() == nullptr) {
@@ -329,7 +340,9 @@ bool HasExternalDataInFile(const ONNX_NAMESPACE::TensorProto& tensor_proto) {
 
 Status TensorProtoWithExternalDataToTensorProto(
     const ONNX_NAMESPACE::TensorProto& ten_proto,
+#ifndef __wasm__
     const std::filesystem::path& model_path,
+#endif
     ONNX_NAMESPACE::TensorProto& new_tensor_proto) {
   // Check if the input tensor has external data
   ORT_RETURN_IF_NOT(HasExternalData(ten_proto), "Input tensor does not have external data.");
@@ -370,30 +383,48 @@ Status TensorProtoWithExternalDataToTensorProto(
   return Status::OK();
 }
 
+#ifndef __wasm__
 // Wraps std::filesystem::weakly_canonical with error_code handling.
+#endif
+#ifndef __wasm__
 static Status WeaklyCanonicalPath(const std::filesystem::path& path, std::filesystem::path& result) {
+#endif
   std::error_code ec;
+#ifndef __wasm__
   result = std::filesystem::weakly_canonical(path, ec);
+#endif
   ORT_RETURN_IF(ec, "Failed to get the weakly canonical path: ", path, " - ", ec.message());
   return Status::OK();
 }
 
+#ifndef __wasm__
 // Wraps std::filesystem::exists with error_code handling.
+#endif
+#ifndef __wasm__
 static Status PathExists(const std::filesystem::path& path, bool& exists) {
+#endif
   std::error_code ec;
+#ifndef __wasm__
   exists = std::filesystem::exists(path, ec);
+#endif
   ORT_RETURN_IF(ec, "Failed to check existence of path: ", path, " - ", ec.message());
   return Status::OK();
 }
 
 // Checks whether `path` has the given path prefix.
+#ifndef __wasm__
 static bool HasPathComponentPrefix(const std::filesystem::path& prefix, const std::filesystem::path& path) {
+#endif
   auto [prefix_end, path_it] = std::mismatch(prefix.begin(), prefix.end(), path.begin(), path.end());
   return prefix_end == prefix.end();
 }
 
+#ifndef __wasm__
 Status ValidateExternalDataPath(const std::filesystem::path& model_path,
+#endif
+#ifndef __wasm__
                                 const std::filesystem::path& external_data_path) {
+#endif
   ORT_RETURN_IF(external_data_path.empty(), "Empty external data path not allowed");
 
   // Note: Use !root_path().empty() to reject paths like '/some/path` even on Windows.
@@ -401,7 +432,9 @@ Status ValidateExternalDataPath(const std::filesystem::path& model_path,
 
 #if defined(__wasm__)
   std::error_code error_code;
+#ifndef __wasm__
   std::filesystem::current_path(error_code);
+#endif
   if (error_code) {
     // If we can't access the current working directory in a WASM build, we assume that the WASM
     // environment does not have a virtual filesystem and defer validation to an ExternalDataLoader for
@@ -412,14 +445,22 @@ Status ValidateExternalDataPath(const std::filesystem::path& model_path,
 
   // Determine the model directory: use model file's parent directory if provided,
   // otherwise use the current working directory.
+#ifndef __wasm__
   std::filesystem::path model_dir = model_path.empty() || model_path.parent_path().empty()
+#endif
+#ifndef __wasm__
                                         ? std::filesystem::path{"."}
+#endif
                                         : model_path.parent_path();
 
   // Resolve the model directory and the external data path to their weakly canonical forms, which
   // resolves symlinks but does not require that the paths actually exist yet.
+#ifndef __wasm__
   std::filesystem::path model_dir_canonical;
+#endif
+#ifndef __wasm__
   std::filesystem::path external_data_path_canonical;
+#endif
   ORT_RETURN_IF_ERROR(WeaklyCanonicalPath(model_dir, model_dir_canonical));
   ORT_RETURN_IF_ERROR(WeaklyCanonicalPath(model_dir_canonical / external_data_path, external_data_path_canonical));
 
@@ -445,7 +486,9 @@ Status ValidateExternalDataPath(const std::filesystem::path& model_path,
   // This supports symlinked models (e.g., Hugging Face Hub local cache) where the canonical
   // parent of the model file differs from the parent directory of the symlinked model file.
   std::error_code ec;
+#ifndef __wasm__
   if (!std::filesystem::is_symlink(model_path, ec)) {
+#endif
     // Note: is_symlink returns false if file is not a symlink, file does not exist, or an error
     // occurred (e.g., permissions). In any of these cases, we just return an error.
     std::string fs_error_msg;
@@ -459,7 +502,9 @@ Status ValidateExternalDataPath(const std::filesystem::path& model_path,
                            external_data_path_canonical, " ", "model directory: ", model_dir, fs_error_msg);
   }
 
+#ifndef __wasm__
   std::filesystem::path real_model_path;
+#endif
   ORT_RETURN_IF_ERROR(WeaklyCanonicalPath(model_path, real_model_path));
   auto real_model_dir = real_model_path.parent_path();
 
@@ -479,7 +524,9 @@ Status ValidateExternalDataPath(const std::filesystem::path& model_path,
 }
 
 Status GetExternalDataInfo(const ONNX_NAMESPACE::TensorProto& tensor_proto,
+#ifndef __wasm__
                            const std::filesystem::path& tensor_proto_dir,
+#endif
                            std::basic_string<ORTCHAR_T>& external_file_path,
                            onnxruntime::FileOffsetType& file_offset,
                            SafeInt<size_t>& tensor_byte_size,
@@ -496,7 +543,9 @@ Status GetExternalDataInfo(const ONNX_NAMESPACE::TensorProto& tensor_proto,
   const auto& location = external_data_info->GetRelPath();
 
   external_file_path = ((location == kTensorProtoLittleEndianMemoryAddressTag) || (location == kTensorProtoNativeEndianMemoryAddressTag))
+#ifndef __wasm__
                            ? std::filesystem::path(location)
+#endif
                            : (tensor_proto_dir / location);
 
   ORT_RETURN_IF_ERROR(GetSizeInBytesFromTensorProto<0>(tensor_proto, &tensor_byte_size));
@@ -648,7 +697,9 @@ void ConvertRawDataInTensorProto(TensorProto& tensor) {
 #if !defined(ORT_MINIMAL_BUILD)
 
 static Status UnpackTensorWithExternalDataImpl(const ONNX_NAMESPACE::TensorProto& tensor,
+#ifndef __wasm__
                                                const std::filesystem::path& tensor_proto_dir,
+#endif
                                                size_t expected_num_elements, size_t element_size,
                                                /*out*/ unsigned char* p_data) {
   ORT_RETURN_IF(nullptr == p_data, "nullptr == p_data");
@@ -664,7 +715,9 @@ static Status UnpackTensorWithExternalDataImpl(const ONNX_NAMESPACE::TensorProto
 
 template <typename T>
 Status UnpackTensorWithExternalData(const ONNX_NAMESPACE::TensorProto& tensor,
+#ifndef __wasm__
                                     const std::filesystem::path& tensor_proto_dir, size_t expected_num_elements,
+#endif
                                     /*out*/ T* p_data) {
   static_assert(std::is_trivially_copyable<T>::value, "T must be trivially copyable");
 
@@ -675,7 +728,9 @@ Status UnpackTensorWithExternalData(const ONNX_NAMESPACE::TensorProto& tensor,
 #define DEFINE_4BIT_UNPACK_TENSOR_WITH_EXT_DATA_IMPL(FOUR_BIT_TYPE, CalcPairFun)                                    \
   template <>                                                                                                       \
   Status UnpackTensorWithExternalData<FOUR_BIT_TYPE>(const ONNX_NAMESPACE::TensorProto& tensor,                     \
+#ifndef __wasm__
                                                      const std::filesystem::path& tensor_proto_dir,                 \
+#endif
                                                      size_t expected_num_elements, /*out*/ FOUR_BIT_TYPE* p_data) { \
     static_assert(std::is_trivially_copyable<FOUR_BIT_TYPE>::value, "T must be trivially copyable");                \
                                                                                                                     \
@@ -707,7 +762,9 @@ DEFINE_4BIT_UNPACK_TENSOR_WITH_EXT_DATA_IMPL(Float4E2M1x2, CalcNumFloat4Pairs)
 #endif
 
 #define INSTANTIATE_UNPACK_EXTERNAL_TENSOR(type)                                                                 \
+#ifndef __wasm__
   template Status UnpackTensorWithExternalData(const ONNX_NAMESPACE::TensorProto&, const std::filesystem::path&, \
+#endif
                                                size_t, type*);
 
 INSTANTIATE_UNPACK_EXTERNAL_TENSOR(float)
@@ -733,7 +790,9 @@ INSTANTIATE_UNPACK_EXTERNAL_TENSOR(Float8E5M2FNUZ)
 
 template <>
 Status UnpackTensorWithExternalData(const ONNX_NAMESPACE::TensorProto& /*tensor*/,
+#ifndef __wasm__
                                     const std::filesystem::path& /*tensor_proto_dir*/, size_t /*expected_num_elements*/,
+#endif
                                     /*out*/ std::string* /*p_data*/) {
   return ORT_MAKE_STATUS(ONNXRUNTIME, FAIL, "External data type cannot be STRING.");
 }
@@ -1162,7 +1221,9 @@ Status UnpackTensor(const ONNX_NAMESPACE::TensorProto& tensor, const void* raw_d
 // Uses the model path to construct the full path for loading external data. In case when model_path is empty
 // it uses current directory.
 template <typename T>
+#ifndef __wasm__
 Status UnpackTensor(const ONNX_NAMESPACE::TensorProto& tensor, const std::filesystem::path& model_path,
+#endif
                     /*out*/ T* p_data, size_t expected_num_elements) {
 #if !defined(ORT_MINIMAL_BUILD)
   if (HasExternalData(tensor)) {
@@ -1181,7 +1242,9 @@ Status UnpackTensor(const ONNX_NAMESPACE::TensorProto& tensor, const std::filesy
 
 // instantiate the UnpackTensor variant that supports external data
 #define INSTANTIATE_UNPACK_TENSOR(type) \
+#ifndef __wasm__
   template Status UnpackTensor(const ONNX_NAMESPACE::TensorProto&, const std::filesystem::path&, type* p_data, size_t);
+#endif
 
 INSTANTIATE_UNPACK_TENSOR(float)
 INSTANTIATE_UNPACK_TENSOR(double)
@@ -1486,12 +1549,18 @@ ORT_API(void, OrtUninitializeBuffer, _In_opt_ void* input, size_t input_len, enu
 #endif
 
 #if !defined(__wasm__)
+#ifndef __wasm__
 static Status GetFileContent(const Env& env, const std::filesystem::path& file_path, FileOffsetType offset,
+#endif
                              size_t length, IAllocatorUniquePtr<void>& external_data) {
   // query length if it is 0
   if (length == 0) {
+#ifndef __wasm__
     // The return type of std::filesystem::file_size is uintmax_t which could be bigger than size_t
+#endif
+#ifndef __wasm__
     length = narrow<size_t>(std::filesystem::file_size(file_path));
+#endif
   }
 
   // first, try to map into memory
@@ -1518,7 +1587,9 @@ static Status GetFileContent(const Env& env, const std::filesystem::path& file_p
 #endif
 
 Status GetExtDataFromTensorProto(const Env& env,
+#ifndef __wasm__
                                  const std::filesystem::path& model_path,
+#endif
                                  const ONNX_NAMESPACE::TensorProto& tensor_proto,
                                  OrtValue& ort_value, PrepackedWeightsForGraph* prepacked_info) {
   ORT_ENFORCE(HasExternalData(tensor_proto), "TensorProto for: ",
@@ -1613,7 +1684,9 @@ Status GetExtDataFromTensorProto(const Env& env,
 #else
     //  The GetFileContent function doesn't report error if the requested data range is invalid. Therefore we need to
     //  manually check file size first.
+#ifndef __wasm__
     std::uintmax_t file_length = std::filesystem::file_size(external_data_file_path);
+#endif
 
     SafeInt<FileOffsetType> end_of_read(file_offset);
     end_of_read += raw_data_safe_len;
@@ -1682,7 +1755,9 @@ Status GetExtDataFromTensorProto(const Env& env,
   return Status::OK();
 }
 
+#ifndef __wasm__
 Status LoadExtDataToTensorFromTensorProto(const Env& env, const std::filesystem::path& model_path,
+#endif
                                           const ONNX_NAMESPACE::TensorProto& tensor_proto,
                                           const IExternalDataLoader& ext_data_loader,
                                           Tensor& tensor) {
@@ -1721,7 +1796,9 @@ Status LoadExtDataToTensorFromTensorProto(const Env& env, const std::filesystem:
  * @param tensor        pre-allocated tensor object, where we store the data
  * @return
  */
+#ifndef __wasm__
 Status TensorProtoToTensor(const Env& env, const std::filesystem::path& model_path,
+#endif
                            const ONNX_NAMESPACE::TensorProto& tensor_proto, Tensor& tensor) {
   // Validate tensor compatibility
   TensorShape tensor_shape = GetTensorShapeFromTensorProto(tensor_proto);
@@ -1825,7 +1902,9 @@ Status TensorProtoToTensor(const Env& env, const std::filesystem::path& model_pa
   return Status::OK();
 }
 
+#ifndef __wasm__
 common::Status CreateTensorFromTensorProto(const Env& env, const std::filesystem::path& model_path,
+#endif
                                            const ONNX_NAMESPACE::TensorProto& tensor_proto, Tensor& tensor) {
   ORT_RETURN_IF_NOT(utils::HasDataType(tensor_proto), "Initializer must have a datatype");
 
@@ -1856,12 +1935,16 @@ Status GetTensorProtoWithDataIfInMemory(
   return Status::OK();
 }
 
+#ifndef __wasm__
 Status TensorProtoToOrtValue(const Env& env, const std::filesystem::path& model_path,
+#endif
                              const ONNX_NAMESPACE::TensorProto& tensor_proto, const MemBuffer& m, OrtValue& value) {
   return TensorProtoToOrtValueImpl(env, model_path, tensor_proto, &m, nullptr, value);
 }
 
+#ifndef __wasm__
 Status TensorProtoToOrtValue(const Env& env, const std::filesystem::path& model_path,
+#endif
                              const ONNX_NAMESPACE::TensorProto& tensor_proto, AllocatorPtr alloc, OrtValue& value) {
   return TensorProtoToOrtValueImpl(env, model_path, tensor_proto, nullptr, alloc, value);
 }
@@ -1969,7 +2052,9 @@ ONNX_NAMESPACE::TypeProto TypeProtoFromTensorProto(const ONNX_NAMESPACE::TensorP
 }
 
 common::Status ConstantNodeProtoToTensorProto(const ONNX_NAMESPACE::NodeProto& node,
+#ifndef __wasm__
                                               const std::filesystem::path& model_path,
+#endif
                                               ONNX_NAMESPACE::TensorProto& tensor, const std::string& tensor_name) {
   ORT_RETURN_IF_NOT(node.attribute_size() > 0, "Constant node: ", node.name(), " has no data attributes");
 
@@ -2028,7 +2113,9 @@ common::Status ConstantNodeProtoToTensorProto(const ONNX_NAMESPACE::NodeProto& n
 }
 
 common::Status ConstantNodeProtoToTensorProto(const ONNX_NAMESPACE::NodeProto& node,
+#ifndef __wasm__
                                               const std::filesystem::path& model_path,
+#endif
                                               ONNX_NAMESPACE::TensorProto& tensor) {
   ORT_ENFORCE(node.output_size() == 1, "NodeProto for Constant should have 1 output. Got:", node.output_size());
   return ConstantNodeProtoToTensorProto(node, model_path, tensor, node.output(0));
@@ -2047,7 +2134,9 @@ void MakeCpuTensorCopy(const Tensor& src_tensor, Tensor& dst_tensor) {
 static Status CopySparseData(const std::string& name,
                              int64_t nnz_elements,
                              const ONNX_NAMESPACE::TensorProto& indices,
+#ifndef __wasm__
                              const std::filesystem::path& model_path,
+#endif
                              gsl::span<const int64_t> dense_dims,
                              int64_t dense_elements,
                              std::function<void(size_t from_idx, size_t to_idx)> copier) {
@@ -2193,7 +2282,9 @@ static Status CopySparseData(const std::string& name,
 }
 
 common::Status SparseTensorProtoToDenseTensorProto(const ONNX_NAMESPACE::SparseTensorProto& sparse,
+#ifndef __wasm__
                                                    const std::filesystem::path& model_path,
+#endif
                                                    ONNX_NAMESPACE::TensorProto& dense) {
   Status status;
 
@@ -2452,7 +2543,9 @@ static void SparsifyGeneric(const void* dense_raw_data, size_t n_dense_elements,
 }
 
 common::Status DenseTensorToSparseTensorProto(const ONNX_NAMESPACE::TensorProto& dense_proto,
+#ifndef __wasm__
                                               const std::filesystem::path& model_path,
+#endif
                                               ONNX_NAMESPACE::SparseTensorProto& result) {
   ORT_ENFORCE(HasDataType(dense_proto), "Must have a valid data type");
 
@@ -2561,7 +2654,9 @@ template common::Status GetSizeInBytesFromTensorProto<0>(const ONNX_NAMESPACE::T
   }
 
 Status UnpackInitializerData(const onnx::TensorProto& initializer,
+#ifndef __wasm__
                              const std::filesystem::path& model_path,
+#endif
                              std::vector<uint8_t>& unpacked_tensor) {
   // TODO, if std::vector does not use a custom allocator, the default std::allocator will
   // allocation the memory aligned to std::max_align_t, need look into allocating
@@ -2613,7 +2708,9 @@ Status UnpackInitializerData(const onnx::TensorProto& initializer,
 Status UnpackInitializerData(const ONNX_NAMESPACE::TensorProto& initializer, std::vector<uint8_t>& unpacked_tensor) {
   ORT_RETURN_IF(initializer.data_location() == TensorProto_DataLocation_EXTERNAL,
                 "The given initializer contains external data");
+#ifndef __wasm__
   return UnpackInitializerData(initializer, std::filesystem::path(), unpacked_tensor);
+#endif
 }
 
 std::optional<std::string> GetNodeProtoLayeringAnnotation(const ONNX_NAMESPACE::NodeProto& node_proto) {
diff --git a/onnxruntime/core/graph/graph.cc b/onnxruntime/core/graph/graph.cc
index 88a1ee0..ac67bba 100644
--- a/onnxruntime/core/graph/graph.cc
+++ b/onnxruntime/core/graph/graph.cc
@@ -5062,12 +5062,16 @@ ONNX_NAMESPACE::GraphProto Graph::ToGraphProtoWithExternalInitializers(
   const std::filesystem::path modified_external_file_path = model_file_path.parent_path() / external_file_path;
   const auto& model_path = ModelPath();
 
+#ifndef __wasm__
   std::ofstream external_stream;
+#endif
   std::streampos external_empty_pos;
   int64_t external_offset = 0;
   if (!model_saving_options.force_embed_external_ini) {
     // Create the external file.
+#ifndef __wasm__
     external_stream.open(modified_external_file_path, std::ofstream::out | std::ofstream::binary);
+#endif
     external_empty_pos = external_stream.tellp();
     ORT_ENFORCE(external_stream.is_open(), "Failed to open for writing:", modified_external_file_path);
   }
diff --git a/onnxruntime/core/session/environment.cc b/onnxruntime/core/session/environment.cc
index 2cf3af8..d0ee33d 100644
--- a/onnxruntime/core/session/environment.cc
+++ b/onnxruntime/core/session/environment.cc
@@ -498,17 +498,17 @@ Status Environment::GetSharedAllocator(const OrtMemoryInfo& mem_info, OrtAllocat
 }
 
 OrtKeyValuePairs Environment::GetConfigEntries() const {
-  std::shared_lock<std::shared_mutex> lock{config_entries_mutex_};
+  std::lock_guard<std::mutex> lock{config_entries_mutex_};
   return config_entries_;  // copy
 }
 
 void Environment::InsertOrAssignConfigEntry(std::string key, std::string value) {
-  std::lock_guard<std::shared_mutex> lock{config_entries_mutex_};
+  std::lock_guard<std::mutex> lock{config_entries_mutex_};
   config_entries_.Add(std::move(key), std::move(value));
 }
 
 void Environment::RemoveConfigEntry(const std::string& key) {
-  std::lock_guard<std::shared_mutex> lock{config_entries_mutex_};
+  std::lock_guard<std::mutex> lock{config_entries_mutex_};
   config_entries_.Remove(key.c_str());
 }
 
diff --git a/onnxruntime/core/session/inference_session.cc b/onnxruntime/core/session/inference_session.cc
index 5436f0c..2aaf702 100644
--- a/onnxruntime/core/session/inference_session.cc
+++ b/onnxruntime/core/session/inference_session.cc
@@ -998,7 +998,9 @@ common::Status InferenceSession::SaveToOrtFormat(const std::filesystem::path& fi
   builder.Finish(session, fbs::InferenceSessionIdentifier());
 
   {
+#ifndef __wasm__
     std::ofstream file(filepath, std::ios::binary);
+#endif
     uint8_t* buf = builder.GetBufferPointer();
     int size = builder.GetSize();
     file.write(reinterpret_cast<const char*>(buf), size);
diff --git a/onnxruntime/core/session/model_compilation_options.cc b/onnxruntime/core/session/model_compilation_options.cc
index efaf28f..4d1c635 100644
--- a/onnxruntime/core/session/model_compilation_options.cc
+++ b/onnxruntime/core/session/model_compilation_options.cc
@@ -34,7 +34,9 @@ ModelCompilationOptions::ModelCompilationOptions(const onnxruntime::Environment&
   session_options_.value.graph_optimization_level = TransformerLevel::Default;  // L0: required transformers only
 }
 
+#ifndef __wasm__
 void ModelCompilationOptions::SetInputModelPath(const std::filesystem::path& input_model_path) {
+#endif
   ResetInputModelSettings();
   input_model_path_ = input_model_path;
 }
@@ -50,7 +52,9 @@ void ModelCompilationOptions::SetInputModel(const OrtModel* model) {
   input_model_ = model;
 }
 
+#ifndef __wasm__
 Status ModelCompilationOptions::SetOutputModelPath(const std::filesystem::path& output_model_path) {
+#endif
   if (output_model_path.empty()) {
     return ORT_MAKE_STATUS(ONNXRUNTIME, INVALID_ARGUMENT, "Output model path must not be empty.");
   }
@@ -94,7 +98,9 @@ Status ModelCompilationOptions::SetOutputModelPath(const std::filesystem::path&
 }
 
 void ModelCompilationOptions::SetOutputModelExternalInitializersFile(
+#ifndef __wasm__
     const std::filesystem::path& external_initializers_path,
+#endif
     size_t external_initializer_size_threshold) {
   session_options_.value.ep_context_gen_options.initializers_location = epctx::ExternalInitializerFileInfo{
       external_initializers_path,
@@ -129,8 +135,12 @@ void ModelCompilationOptions::SetOutputModelGetInitializerLocationFunc(
   };
 }
 
+#ifndef __wasm__
 Status ModelCompilationOptions::SetEpContextBinaryInformation(const std::filesystem::path& output_directory,
+#endif
+#ifndef __wasm__
                                                               const std::filesystem::path& model_name) {
+#endif
   if (output_directory.empty() || model_name.empty()) {
     return ORT_MAKE_STATUS(ONNXRUNTIME, INVALID_ARGUMENT, "output_dir or model_name is empty.");
   }
@@ -139,7 +149,9 @@ Status ModelCompilationOptions::SetEpContextBinaryInformation(const std::filesys
     return ORT_MAKE_STATUS(ONNXRUNTIME, INVALID_ARGUMENT, "output_dir is not a valid directory.");
   }
 
+#ifndef __wasm__
   std::filesystem::path ctx_model_path = output_directory / model_name;
+#endif
   std::string ctx_model_path_str = PathToUTF8String(ctx_model_path);
 
   if (ctx_model_path_str.size() <= ConfigOptions::kMaxValueLength) {
@@ -183,7 +195,9 @@ bool ModelCompilationOptions::InputModelComesFromFile() const {
   return !input_model_path_.empty();
 }
 
+#ifndef __wasm__
 const std::filesystem::path& ModelCompilationOptions::GetInputModelPath() const {
+#endif
   return input_model_path_;
 }
 
@@ -264,7 +278,9 @@ Status ModelCompilationOptions::Check() const {
                            "file path, memory buffer, or OrtModel");
   }
 
+#ifndef __wasm__
   if (input_from_file && !std::filesystem::exists(input_model_path_)) {
+#endif
     return ORT_MAKE_STATUS(ONNXRUNTIME, INVALID_ARGUMENT, "Input model path does not exist: ", input_model_path_);
   }
 
diff --git a/onnxruntime/core/session/ort_version_check.h b/onnxruntime/core/session/ort_version_check.h
index 82fd757..8c9b39a 100644
--- a/onnxruntime/core/session/ort_version_check.h
+++ b/onnxruntime/core/session/ort_version_check.h
@@ -10,21 +10,21 @@
 
 namespace onnxruntime::version_check {
 
-// A simple consteval-friendly result type for ParseUint.
-// std::optional triggers an internal compiler error in MSVC 14.44 when used with consteval.
+// A simple constexpr-friendly result type for ParseUint.
+// std::optional triggers an internal compiler error in MSVC 14.44 when used with constexpr.
 struct ParseUintResult {
   uint32_t value;
   bool has_value;
 
-  consteval bool operator==(uint32_t other) const { return has_value && value == other; }
-  consteval bool operator!=(uint32_t other) const { return !(*this == other); }
+  constexpr bool operator==(uint32_t other) const { return has_value && value == other; }
+  constexpr bool operator!=(uint32_t other) const { return !(*this == other); }
 };
 
-inline consteval ParseUintResult ParseUintNone() { return {0, false}; }
+inline constexpr ParseUintResult ParseUintNone() { return {0, false}; }
 
 // Parse a non-negative integer from a string_view without leading zeros.
 // Returns a result with has_value == false on failure (empty, leading zero, non-digit, or overflow).
-consteval ParseUintResult ParseUint(std::string_view str) {
+constexpr ParseUintResult ParseUint(std::string_view str) {
   if (str.empty()) return ParseUintNone();
   // Leading zeros are not allowed (except "0" itself).
   if (str.size() > 1 && str[0] == '0') return ParseUintNone();
@@ -42,7 +42,7 @@ consteval ParseUintResult ParseUint(std::string_view str) {
 //   - Major version is 1
 //   - Y and Z are non-negative integers without leading zeros
 //   - Y (minor version) must equal expected_api_version (defaults to ORT_API_VERSION)
-consteval bool IsOrtVersionValid(std::string_view version, uint32_t expected_api_version = ORT_API_VERSION) {
+constexpr bool IsOrtVersionValid(std::string_view version, uint32_t expected_api_version = ORT_API_VERSION) {
   size_t first_dot = version.find('.');
   if (first_dot == std::string_view::npos) return false;
   size_t second_dot = version.find('.', first_dot + 1);
